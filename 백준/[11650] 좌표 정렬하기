import sys
input = sys.stdin.readline

N = int(input())
info = []

for _ in range(N):
    x, y = map(int, input().split())
    info.append((x,y))

info.sort()

for i in info:
    print(' '.join(map(str, i)))
