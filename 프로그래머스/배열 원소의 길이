class Solution {
    public int[] solution(String[] strlist) {
        int[] answer = new int[strlist.length];
        int idx = 0;
        
        for (String s:strlist){
            int len = s.length();
            answer[idx] = len;
            idx++;
        }
        return answer;
    }
}
